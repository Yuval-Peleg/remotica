# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea

# Node (safety net — frontend/ has its own .gitignore too)
node_modules/
dist/
dist-ssr/
*.local
*.log

# Old static mockup, kept locally for reference only — not part of the repo
/legacy-static/
/images/

# C backend build output and runtime-generated data (profile, uploads)
/backend/build/
/backend/data/
